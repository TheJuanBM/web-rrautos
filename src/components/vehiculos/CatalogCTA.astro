---
interface Props {
  primaryHref: string
  primaryLabel: string
  secondaryHref: string
  secondaryLabel: string
}

const { primaryHref, primaryLabel, secondaryHref, secondaryLabel } = Astro.props
---

<section class="catalog__cta" aria-labelledby="cta-title">
  <div class="cta-card">
    <div class="cta-card__content">
      <p class="section-eyebrow">Reserva sin compromiso</p>
      <h2 id="cta-title">Agenda un test drive y asegura tu auto ideal</h2>
      <p>
        Coordina tu visita presencial o virtual. Te mostramos el estado real de la unidad, simulamos financiamiento y
        bloqueamos el vehículo mientras firmas.
      </p>
    </div>
    <div class="cta-card__actions">
      <a class="cta-btn cta-btn--primary" href={primaryHref} rel="noopener">{primaryLabel}</a>
      <a class="cta-btn cta-btn--ghost" href={secondaryHref}>{secondaryLabel}</a>
    </div>
  </div>
</section>

<style>
  .catalog__cta .cta-card {
    background: linear-gradient(140deg, var(--color-primary-hover), var(--color-primary-contrast));
    color: var(--color-white);
    border-radius: 1.75rem;
    padding: clamp(2rem, 4.5vw, 3rem);
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 1.5rem;
    box-shadow: 0 35px 65px -42px rgba(var(--color-primary-shadow-rgb), 0.8);
  }

  .cta-card__content {
    display: grid;
    gap: 0.75rem;
    max-width: 32rem;
  }

  .cta-card__content h2 {
    color: var(--color-white);
  }

  .cta-card__content p {
    color: rgba(var(--color-gray-250-rgb), 0.85);
    margin: 0;
  }

  .cta-card__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    align-items: center;
  }

  .cta-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    border-radius: 9999px;
    padding: 0.9rem 1.6rem;
    font-weight: 600;
    font-size: 0.98rem;
    text-decoration: none;
    transition:
      transform 150ms ease,
      box-shadow 150ms ease,
      background-color 150ms ease,
      color 150ms ease;
  }

  .cta-btn--primary {
    background: var(--color-white);
    color: var(--color-primary-hover);
    box-shadow: 0 15px 40px -18px rgba(var(--color-white-rgb), 0.9);
  }

  .cta-btn--primary:hover,
  .cta-btn--primary:focus-visible {
    transform: translateY(-1px);
    box-shadow: 0 18px 45px -16px rgba(var(--color-white-rgb), 0.85);
  }

  .cta-btn--ghost {
    border: 1px solid rgba(var(--color-gray-250-rgb), 0.4);
    color: var(--color-gray-250);
  }

  .cta-btn--ghost:hover,
  .cta-btn--ghost:focus-visible {
    background: rgba(var(--color-gray-250-rgb), 0.18);
  }

  @media (max-width: 900px) {
    .catalog__cta .cta-card {
      padding: clamp(1.2rem, 4vw, 1.7rem);
    }
  }

  @media (max-width: 640px) {
    .cta-card__actions {
      width: 100%;
      gap: 0.6rem;
    }

    .cta-card__actions .cta-btn {
      width: 100%;
      justify-content: center;
    }
  }

  @media (max-width: 480px) {
    .catalog__cta .cta-card {
      padding: clamp(1.1rem, 6vw, 1.5rem);
      border-radius: 1.25rem;
    }

    .cta-card__actions {
      gap: 0.5rem;
    }
  }
</style>
