---
import Layout from '../layouts/Layout.astro'

Astro.response.status = 404
export const prerender = true
---

<Layout title="Página no encontrada">
  <section class="not-found" aria-labelledby="not-found-title">
    <div class="not-found__glow" aria-hidden="true"></div>
    <div class="not-found__inner">
      <div class="not-found__badge">
        <span aria-hidden="true">404</span>
        <span class="text-white">Página no encontrada</span>
      </div>

      <h1 id="not-found-title">Lo sentimos, no pudimos encontrar lo que buscabas</h1>
      <p>
        Es posible que el enlace haya caducado o que la página se haya movido. Volvamos al camino correcto para que
        sigas descubriendo autos y soluciones pensadas para ti.
      </p>

      <div class="not-found__cta-group">
        <a class="not-found__cta not-found__cta--primary" href="/">Ir al inicio</a>
        <a class="not-found__cta not-found__cta--secondary" href="/vehiculos">Ver catálogo de vehículos</a>
        <a class="not-found__link" href="https://wa.me/573002006818" rel="noopener">Hablar con un asesor</a>
      </div>
    </div>
  </section>

  <style>
    :global(body) {
      background:
        radial-gradient(circle at top left, rgba(var(--color-primary-glow-rgb), 0.12), transparent 55%),
        linear-gradient(180deg, rgba(var(--surface-panel-rgb), 0.8) 0%, rgba(var(--surface-body-rgb), 0.96) 70%);
    }

    .not-found {
      position: relative;
      min-height: calc(100vh - 140px);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: clamp(4rem, 8vw, 6rem) clamp(1.75rem, 4vw, 3rem);
      overflow: hidden;
    }

    .not-found__glow {
      position: absolute;
      inset: 0;
      pointer-events: none;
      background:
        radial-gradient(circle at 20% 20%, rgba(var(--color-primary-glow-rgb), 0.2), transparent 55%),
        radial-gradient(circle at 80% 75%, rgba(var(--color-secondary-glow-rgb), 0.18), transparent 55%),
        linear-gradient(160deg, rgba(var(--surface-card-rgb), 0.3), transparent 70%);
      filter: blur(45px);
      opacity: 0.75;
    }

    .not-found__inner {
      position: relative;
      z-index: 1;
      width: min(760px, 100%);
      background:
        linear-gradient(135deg, rgba(var(--surface-card-rgb), 0.92), rgba(var(--surface-card-rgb), 0.85)),
        linear-gradient(225deg, rgba(var(--color-primary-soft-rgb), 0.04), transparent 55%);
      border: 1px solid rgba(var(--surface-border-rgb), 0.35);
      box-shadow:
        0 30px 80px -32px rgba(var(--surface-panel-rgb), 0.55),
        inset 0 1px 0 rgba(var(--color-white-rgb), 0.12);
      border-radius: 2.5rem;
      backdrop-filter: blur(18px);
      -webkit-backdrop-filter: blur(18px);
      padding: clamp(2.8rem, 6vw, 4rem);
      display: grid;
      gap: clamp(1.5rem, 4vw, 2.5rem);
      text-align: center;
    }

    .not-found__badge {
      display: inline-flex;
      align-items: center;
      gap: 0.8rem;
      margin: 0 auto;
      padding: 1rem;
      border-radius: 9999px;
      background: var(--color-gray-600);
      border: 1px solid rgba(var(--surface-panel-rgb), 0.25);
      font-weight: 600;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      font-size: 0.78rem;
      color: rgba(var(--surface-panel-rgb), 0.9);
    }

    .not-found__badge span:first-child {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.5rem;
      border-radius: 9999px;
      background: var(--color-white);
      box-shadow: 0 8px 18px -10px rgba(var(--color-primary-soft-rgb), 0.55);
      color: var(--surface-panel);
      font-weight: 700;
      font-size: 1rem;
    }

    .not-found h1 {
      margin: 0;
      font-size: clamp(2rem, 6vw, 3.2rem);
      line-height: 1.12;
      color: var(--text-primary);
      letter-spacing: -0.015em;
    }

    .not-found p {
      margin: 0;
      font-size: 1.05rem;
      line-height: 1.7;
      color: var(--text-secondary);
    }

    .not-found__cta-group {
      display: grid;
      gap: 0.75rem;
      justify-items: center;
    }

    .not-found__cta {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.9rem 1.8rem;
      border-radius: 9999px;
      font-weight: 600;
      font-size: 0.98rem;
      text-decoration: none;
      transition:
        transform 160ms ease,
        box-shadow 160ms ease,
        background-color 160ms ease,
        color 160ms ease;
    }

    .not-found__cta--primary {
      width: 100%;
      color: var(--surface-panel);
      background: linear-gradient(135deg, rgba(var(--color-white-rgb), 0.95), rgba(var(--color-primary-soft-rgb), 0.4));
      border: 1px solid rgba(var(--color-white-rgb), 0.75);
      box-shadow: 0 18px 42px -22px rgba(var(--surface-panel-rgb), 0.55);
    }

    .not-found__cta--primary:hover,
    .not-found__cta--primary:focus-visible {
      transform: translateY(-2px);
      box-shadow: 0 24px 55px -24px rgba(var(--surface-panel-rgb), 0.6);
    }

    .not-found__cta--secondary {
      width: 100%;
      color: rgba(var(--color-primary-soft-rgb), 0.9);
      background: rgba(var(--surface-card-rgb), 0.4);
      border: 1px solid rgba(var(--color-primary-soft-rgb), 0.25);
      backdrop-filter: blur(6px);
      -webkit-backdrop-filter: blur(6px);
    }

    .not-found__cta--secondary:hover,
    .not-found__cta--secondary:focus-visible {
      background: rgba(var(--surface-card-rgb), 0.55);
      transform: translateY(-1px);
    }

    .not-found__link {
      margin-top: 0.25rem;
      font-weight: 500;
      color: rgba(var(--color-primary-soft-rgb), 0.85);
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 0.35rem;
    }

    .not-found__link::after {
      content: '↗';
      font-size: 0.85rem;
      transition: transform 150ms ease;
    }

    .not-found__link:hover::after,
    .not-found__link:focus-visible::after {
      transform: translateY(-2px);
    }

    @media (max-width: 720px) {
      .not-found__inner {
        border-radius: 2rem;
        padding: clamp(2.2rem, 7vw, 2.8rem);
      }

      .not-found__cta-group {
        gap: 0.65rem;
      }

      .not-found__cta {
        width: 100%;
      }
    }
  </style>
</Layout>
