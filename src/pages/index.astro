---
import Layout from '../layouts/Layout.astro'
---

<Layout>
  <section class="landing" aria-labelledby="landing-title">
    <div class="landing__glow" aria-hidden="true"></div>

    <div class="landing__content">
      <section class="hero" aria-labelledby="landing-title">
        <div class="hero__copy">
          <span class="hero__eyebrow">Confianza y respaldo garantizados</span>
          <h1 id="landing-title">Tu próximo vehículo te espera en RR Autos</h1>
          <p class="hero__description">
            Descubre un catálogo curado de autos certificados, con financiamiento flexible y asesoría personalizada en
            cada etapa. Compra con transparencia, sin sorpresas y con garantía postventa.
          </p>

          <div class="hero__actions">
            <a class="hero__cta hero__cta--primary" href="/vehiculos">Ver catálogo completo</a>
            <a class="hero__cta hero__cta--ghost" href="https://wa.me/51987654321" rel="noopener"
              >Hablar con un asesor</a
            >
          </div>

          <dl class="hero__stats" aria-label="Indicadores de confianza">
            <div>
              <dt>+200</dt>
              <dd>Autos inspeccionados</dd>
            </div>
            <div>
              <dt>24&nbsp;h</dt>
              <dd>Aprobación promedio de crédito</dd>
            </div>
            <div>
              <dt>100%</dt>
              <dd>Clientes satisfechos con nosotros</dd>
            </div>
          </dl>
        </div>

        <aside class="hero__panel" aria-label="Beneficios destacados">
          <article class="panel panel--accent">
            <h2>Financiamiento flexible</h2>
            <p>
              Trabajamos con bancos y financieras aliadas para ofrecer cuotas a medida y tasas preferenciales desde el
              primer contacto.
            </p>
            <ul>
              <li>Precalificación online sin costo</li>
              <li>Simulación de cuotas en 5 minutos</li>
              <li>Firma digital disponible</li>
            </ul>
          </article>
          <article class="panel">
            <h3>Tu compra incluye</h3>
            <ul>
              <li>Revisión mecánica certificada</li>
              <li>Garantía postventa de 90 días</li>
              <li>Gestión completa de papeleo</li>
            </ul>
          </article>
        </aside>
      </section>

      <section class="features" aria-labelledby="features-title">
        <header class="section-header">
          <p class="section-eyebrow">¿Por qué elegirnos?</p>
          <h2 id="features-title">Una experiencia de compra diseñada para ti</h2>
          <p class="section-description">
            Acompañamos cada decisión con información clara, procesos ágiles y beneficios exclusivos para que disfrutes
            tu vehículo desde el primer día.
          </p>
        </header>

        <div class="features__grid">
          <article class="feature-card">
            <span class="feature-card__icon" aria-hidden="true">
              <svg viewBox="0 0 48 48" role="img" focusable="false">
                <path
                  d="M12 30h24M12 20h24M16 12h16"
                  stroke="currentColor"
                  stroke-width="3"
                  stroke-linecap="round"
                  stroke-linejoin="round"></path>
                <rect
                  x="8"
                  y="16"
                  width="32"
                  height="20"
                  rx="6"
                  ry="6"
                  stroke="currentColor"
                  stroke-width="3"
                  fill="none"></rect>
              </svg>
            </span>
            <h3>Inventario certificado</h3>
            <p>
              Cada vehículo pasa por 150 puntos de inspección, historial verificado y pruebas de manejo supervisadas por
              especialistas.
            </p>
          </article>

          <article class="feature-card">
            <span class="feature-card__icon" aria-hidden="true">
              <svg viewBox="0 0 48 48" role="img" focusable="false">
                <path
                  d="M12 20c0-7 5-12 12-12s12 5 12 12c0 9-12 20-12 20S12 29 12 20Z"
                  stroke="currentColor"
                  stroke-width="3"
                  fill="none"
                  stroke-linejoin="round"></path>
                <circle cx="24" cy="20" r="5" stroke="currentColor" stroke-width="3" fill="none"></circle>
              </svg>
            </span>
            <h3>Transparencia total</h3>
            <p>
              Conoce el historial, mantenimiento y valorización real del auto. Sin letra chica: costo final claro desde
              el inicio.
            </p>
          </article>

          <article class="feature-card">
            <span class="feature-card__icon" aria-hidden="true">
              <svg viewBox="0 0 48 48" role="img" focusable="false">
                <path
                  d="M24 10v14l9 5"
                  stroke="currentColor"
                  stroke-width="3"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  fill="none"></path>
                <circle cx="24" cy="24" r="18" stroke="currentColor" stroke-width="3" fill="none"></circle>
              </svg>
            </span>
            <h3>Proceso ágil</h3>
            <p>
              Agenda tu visita, prueba el vehículo y recibe la documentación lista en menos de 72 horas con nuestra mesa
              de trámites.
            </p>
          </article>
        </div>
      </section>

      <section class="process" aria-labelledby="process-title">
        <header class="section-header">
          <p class="section-eyebrow">Así de simple</p>
          <h2 id="process-title">Te acompañamos en tres pasos</h2>
        </header>

        <ol class="process__steps">
          <li>
            <span class="step__counter">1</span>
            <div class="step__content">
              <h3>Conecta con un asesor</h3>
              <p>
                Cuéntanos lo que buscas y recibe comparativas personalizadas con opciones disponibles en tiempo real.
              </p>
            </div>
          </li>
          <li>
            <span class="step__counter">2</span>
            <div class="step__content">
              <h3>Prueba y elige</h3>
              <p>
                Agenda tu test drive cuando prefieras. Revisamos juntos el informe técnico y resolvemos cualquier duda
                antes de firmar.
              </p>
            </div>
          </li>
          <li>
            <span class="step__counter">3</span>
            <div class="step__content">
              <h3>Entrega inmediata</h3>
              <p>
                Coordinamos matrícula, SOAT y transferencia. Te entregamos las llaves con todo el expediente digital en
                tu correo.
              </p>
            </div>
          </li>
        </ol>
      </section>

      <section class="cta" aria-labelledby="cta-title">
        <div class="cta__content">
          <h2 id="cta-title">Empieza hoy mismo</h2>
          <p>
            Agenda una asesoría virtual gratuita y descubre el auto ideal para ti. Nuestro equipo está listo para
            ayudarte a tomar la mejor decisión.
          </p>
        </div>
        <div class="cta__actions">
          <a class="hero__cta cta-btn--primary" href="https://calendly.com/rr-autos/asesoria" rel="noopener">
            Reservar asesoría
          </a>
        </div>
      </section>
    </div>
  </section>

  <style>
    .landing {
      position: relative;
      min-height: 100vh;
      overflow: hidden;
      padding: clamp(3.5rem, 8vw, 6rem) 0 clamp(4rem, 10vw, 7rem);
      color: var(--text-primary);
      background:
        radial-gradient(circle at top left, rgba(var(--color-primary-glow-rgb), 0.06), transparent 55%),
        linear-gradient(180deg, rgba(var(--surface-panel-rgb), 0.78) 0%, rgba(var(--surface-body-rgb), 0.94) 100%);
    }

    .landing__glow {
      position: absolute;
      inset: auto -20% -40% 40%;
      height: 60vh;
      background: radial-gradient(circle at center, rgba(var(--color-primary-glow-rgb), 0.12), transparent 65%);
      filter: blur(80px);
      opacity: 0.85;
      pointer-events: none;
    }

    .landing__content {
      position: relative;
      z-index: 1;
      width: min(1140px, calc(100% - 3rem));
      margin: 0 auto;
      display: grid;
      gap: clamp(3rem, 8vw, 4.5rem);
    }

    .hero {
      display: grid;
      gap: clamp(2.5rem, 6vw, 3.5rem);
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      align-items: stretch;
    }

    .hero__copy {
      display: grid;
      gap: clamp(1rem, 2.5vw, 1.75rem);
    }

    .hero__eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      width: fit-content;
      padding: 0.35rem 0.85rem;
      border-radius: 9999px;
      background: rgba(var(--color-primary-glow-rgb), 0.1);
      color: rgba(var(--color-primary-soft-rgb), 0.9);
      font-size: 0.8125rem;
      font-weight: 600;
      letter-spacing: 0.04em;
      text-transform: uppercase;
    }

    h1 {
      font-size: clamp(2.25rem, 6vw, 3.5rem);
      line-height: 1.05;
      margin: 0;
      letter-spacing: -0.02em;
    }

    .hero__description {
      font-size: clamp(1rem, 2.25vw, 1.15rem);
      line-height: 1.7;
      max-width: 36rem;
      color: var(--text-secondary);
      margin: 0;
    }

    .hero__actions {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 0.75rem;
    }

    .hero__cta {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      border-radius: 9999px;
      padding: 0.85rem 1.6rem;
      font-weight: 600;
      font-size: 0.98rem;
      text-decoration: none;
      transition:
        transform 150ms ease,
        box-shadow 150ms ease,
        background-color 150ms ease,
        color 150ms ease;
    }

    .hero__cta--primary {
      color: var(--surface-panel);
      background: linear-gradient(135deg, rgba(var(--color-white-rgb), 0.92), rgba(var(--color-primary-soft-rgb), 0.4));
      box-shadow: 0 12px 32px -18px rgba(var(--surface-panel-rgb), 0.45);
    }

    .hero__cta--primary:hover,
    .hero__cta--primary:focus-visible {
      transform: translateY(-1px);
      box-shadow: 0 16px 40px -18px rgba(var(--surface-panel-rgb), 0.55);
    }

    .cta-btn--primary {
      background: rgba(var(--color-primary-soft-rgb), 0.16);
      color: var(--text-primary);
      box-shadow: 0 15px 36px -20px rgba(var(--surface-panel-rgb), 0.45);
    }

    .cta-btn--primary:hover,
    .cta-btn--primary:focus-visible {
      transform: translateY(-1px);
      box-shadow: 0 18px 42px -18px rgba(var(--surface-panel-rgb), 0.5);
    }

    .hero__cta--ghost {
      color: rgba(var(--color-primary-soft-rgb), 0.85);
      background: rgba(var(--surface-card-alt-rgb), 0.35);
      border: 1px solid rgba(var(--color-primary-soft-rgb), 0.22);
    }

    .hero__cta--ghost:hover,
    .hero__cta--ghost:focus-visible {
      background: rgba(var(--surface-card-alt-rgb), 0.5);
    }

    .hero__stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
      gap: 1rem;
      margin: 0;
    }

    .hero__stats div {
      background: rgba(var(--surface-card-rgb), 0.85);
      border: 1px solid var(--surface-border-strong);
      border-radius: 1rem;
      padding: 1rem 1.25rem;
      backdrop-filter: blur(14px);
    }

    .hero__stats dt {
      font-size: 1.75rem;
      font-weight: 700;
      color: rgba(var(--color-primary-soft-rgb), 0.9);
    }

    .hero__stats dd {
      margin: 0.25rem 0 0;
      font-size: 0.95rem;
      color: var(--text-muted);
    }

    .hero__panel {
      display: grid;
      gap: 1rem;
    }

    .panel {
      background: rgba(var(--surface-card-rgb), 0.9);
      border: 1px solid var(--surface-border);
      border-radius: 1.25rem;
      padding: clamp(1.5rem, 3vw, 2rem);
      backdrop-filter: blur(18px);
      box-shadow: 0 18px 45px -30px rgba(var(--surface-panel-rgb), 0.6);
      display: grid;
      gap: 1rem;
    }

    .panel--accent {
      background: linear-gradient(
        140deg,
        rgba(var(--color-primary-glow-rgb), 0.14) 0%,
        rgba(var(--color-primary-soft-rgb), 0.28) 100%
      );
      border: 1px solid rgba(var(--color-primary-hover-rgb), 0.24);
    }

    .panel h2,
    .panel h3 {
      margin: 0;
      font-size: 1.3rem;
      color: var(--text-primary);
    }

    .panel p,
    .panel ul {
      margin: 0;
      color: var(--text-secondary);
      font-size: 0.98rem;
      line-height: 1.6;
    }

    .panel ul {
      display: grid;
      gap: 0.5rem;
      list-style: none;
      padding: 0;
    }

    .panel li {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .panel li::before {
      content: '';
      width: 0.65rem;
      height: 0.65rem;
      border-radius: 999px;
      background: linear-gradient(
        140deg,
        rgba(var(--color-primary-glow-rgb), 0.9),
        rgba(var(--color-primary-hover-rgb), 0.7)
      );
      flex-shrink: 0;
      box-shadow: 0 0 0 3px rgba(var(--color-primary-hover-rgb), 0.14);
    }

    .section-header {
      display: grid;
      gap: 0.5rem;
      max-width: 40rem;
    }

    .section-eyebrow {
      text-transform: uppercase;
      letter-spacing: 0.12em;
      font-size: 0.75rem;
      font-weight: 600;
    }

    .section-description {
      margin: 0;
      color: var(--text-secondary);
    }

    h2 {
      margin: 0;
      font-size: clamp(1.75rem, 4vw, 2.2rem);
      color: var(--text-primary);
    }

    .features__grid {
      margin-top: clamp(1.5rem, 3vw, 2rem);
      display: grid;
      gap: 1.5rem;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    }

    .feature-card {
      background: rgba(var(--surface-card-rgb), 0.92);
      border-radius: 1.25rem;
      padding: 1.75rem;
      border: 1px solid var(--surface-border);
      box-shadow: 0 25px 45px -35px rgba(var(--surface-panel-rgb), 0.55);
      display: grid;
      gap: 1rem;
    }

    .feature-card__icon {
      width: 3.25rem;
      height: 3.25rem;
      display: grid;
      place-items: center;
      border-radius: 1rem;
      background: linear-gradient(
        140deg,
        rgba(var(--color-primary-soft-rgb), 0.2),
        rgba(var(--color-primary-glow-rgb), 0.1)
      );
      color: rgba(var(--color-primary-soft-rgb), 0.9);
    }

    .feature-card__icon svg {
      width: 2rem;
      height: 2rem;
    }

    .feature-card h3 {
      margin: 0;
      font-size: 1.1rem;
      color: var(--text-primary);
    }

    .feature-card p {
      margin: 0;
      color: var(--text-secondary);
      line-height: 1.6;
    }

    .process__steps {
      margin: clamp(2rem, 4vw, 2.5rem) 0 0;
      padding: 0;
      display: grid;
      gap: 1.5rem;
      list-style: none;
    }

    .process__steps li {
      display: grid;
      gap: 0.75rem;
      border-radius: 1.25rem;
      border: 1px solid var(--surface-border);
      background: rgba(var(--surface-card-rgb), 0.9);
      padding: 1.5rem 1.75rem;
      box-shadow: 0 20px 45px -35px rgba(var(--surface-panel-rgb), 0.5);
    }

    .process__steps li:nth-child(2) {
      background: linear-gradient(
        135deg,
        rgba(var(--color-primary-soft-rgb), 0.24),
        rgba(var(--color-primary-glow-rgb), 0.22)
      );
    }

    .step__counter {
      width: 2.25rem;
      height: 2.25rem;
      border-radius: 9999px;
      display: grid;
      place-items: center;
      font-weight: 700;
      color: rgba(var(--color-primary-soft-rgb), 0.92);
      background: rgba(var(--color-primary-soft-rgb), 0.16);
      box-shadow: inset 0 0 0 1px rgba(var(--color-primary-hover-rgb), 0.26);
    }

    .step__content h3 {
      margin: 0;
      font-size: 1.15rem;
    }

    .step__content p {
      margin: 0.5rem 0 0;
      color: var(--text-secondary);
    }

    .cta {
      margin-top: clamp(2.5rem, 6vw, 4rem);
      padding: clamp(2rem, 4vw, 2.75rem);
      border-radius: 1.75rem;
      background: linear-gradient(
        135deg,
        rgba(var(--color-white-rgb), 0.92),
        rgba(var(--color-primary-soft-rgb), 0.26)
      );
      color: var(--surface-panel);
      display: flex;
      flex-wrap: wrap;
      gap: 1.5rem;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 32px 58px -36px rgba(var(--surface-panel-rgb), 0.38);
    }

    .cta__content {
      max-width: 32rem;
      display: grid;
      gap: 0.5rem;
    }

    .cta__content h2 {
      color: rgba(var(--surface-card-rgb), 0.85);
      margin: 0;
    }

    .cta__content p {
      margin: 0;
      color: rgba(var(--surface-card-rgb), 0.75);
    }

    .cta__actions {
      display: flex;
      flex-wrap: wrap;
      gap: 0.75rem;
    }

    .cta .hero__cta--ghost {
      color: rgba(var(--surface-card-rgb), 0.8);
      border-color: rgba(var(--surface-card-rgb), 0.28);
    }

    .cta .hero__cta--ghost:hover,
    .cta .hero__cta--ghost:focus-visible {
      color: var(--surface-panel);
      background: rgba(var(--surface-card-rgb), 0.16);
    }

    @media (max-width: 768px) {
      .hero__stats {
        gap: 0.75rem;
      }

      .hero__stats div {
        padding: 0.85rem 1rem;
      }

      .panel {
        padding: 1.5rem;
      }
    }

    @media (max-width: 640px) {
      .landing {
        padding-left: 1.25rem;
        padding-right: 1.25rem;
      }

      .hero__actions {
        flex-direction: column;
        align-items: stretch;
      }

      .hero__cta {
        width: 100%;
      }

      .process__steps {
        gap: 1rem;
      }

      .process__steps li {
        padding: 1.25rem 1.5rem;
      }

      .cta {
        flex-direction: column;
        align-items: flex-start;
      }

      .cta__actions {
        width: 100%;
      }

      .cta__actions .hero__cta {
        width: 100%;
        justify-content: center;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      .hero__cta,
      .panel,
      .feature-card,
      .process__steps li,
      .cta {
        transition: none;
        box-shadow: none;
      }
    }
  </style>
</Layout>
